<form method="post" action="{{ path('app_detail_achat_delete', {'id': detail_achat.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce Produit ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_achat.id) }}">
    <button class="button">Supprimer</button>
</form>
