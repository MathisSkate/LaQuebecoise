<form method="post" action="{{ path('app_localisation_delete', {'id': location.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette Localisation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location.id) }}">
    <button class="btn">Supprimer</button>
</form>
