{% extends 'base.html.twig' %}

{% block body %}
    <div class="forms">
        <div class="form align-content-center text-center">
            <h1>Modifier une Localisation</h1>

            {{ include('localisation/_form.html.twig', {'button_label': 'Modifier'}) }}
            <br/>
            <a href="{{ path('app_localisation_index') }}"><button class="button">Retour à la liste</button></a>
        </div>
    </div>
{% endblock %}
