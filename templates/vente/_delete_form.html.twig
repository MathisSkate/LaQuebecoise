<form method="post" action="{{ path('app_vente_delete', {'id': vente.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette Vente ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vente.id) }}">
    <button class="button">Supprimer</button>
</form>
