<form method="post" action="{{ path('app_detail_vente_delete', {'id': detail_vente.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce Produit ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_vente.id) }}">
    <button class="button">Supprimer</button>
</form>
