{% extends 'base.html.twig' %}

{% block body %}
    <div class="forms">
        <div class="form align-content-center text-center">
            <h1>Créer une nouvelle Matière Première</h1>

            {{ include('matiere/_form.html.twig') }}
            <br/>
            <a href="{{ path('app_matiere_index') }}"><button class="button">Retour à la liste</button></a>
        </div>
    </div>
{% endblock %}
